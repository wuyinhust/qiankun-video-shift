# 分镜观察规范

本参考只在第二式补全逐镜语义时读取。

## 证据优先级

1. 实际关键帧和对应时间戳。
2. 同镜头首中尾变化、逐镜短片或整段视频。
3. 音轨或转录。
4. 用户提供的真实背景信息。
5. 基于专业知识的推断。

前四项属于证据，第五项必须明确标记为推断。不得从水印、字幕风格或人物外貌擅自识别具体账号、真人、品牌或平台。

## 每镜必填维度

| 维度 | 观察内容 |
|---|---|
| 时间 | `start_s`、`end_s`、持续时间、与前后镜的关系 |
| 画面 | 客观描述当前可见的主体、动作、场景与物体状态 |
| 景别 | 远景、全景、中景、近景、特写、大特写 |
| 机位 | 平视、俯拍、仰拍、鸟瞰、低机位、过肩、POV |
| 构图 | 主体位置、视觉重心、前中后景、留白、对称/三分等 |
| 运镜 | static、pan、tilt、push/pull、tracking、truck、orbit、crane、zoom、handheld、roll |
| 主体运动 | 方向、幅度、速度、起止状态和与道具的交互 |
| 时序 | 实时、慢动作、延时、速度渐变、定格 |
| 光色 | 光源方向、软硬、曝光、主色、饱和度、对比和跨镜一致性 |
| 质感 | 写实、胶片、动画、3D、纪实、商业棚拍等可观察特征 |
| 文字 | 原样记录可辨认的字幕、标签、标识；不清楚则 `unknown` |
| 声音 | 台词、旁白、环境音、动作音、画内音乐、画外配乐；不得猜曲名 |
| 转场 | 硬切、叠化、淡入淡出、匹配剪辑、甩镜、遮挡等 |
| 证据 | 支撑结论的关键帧路径或短片路径 |

## 运镜判别纪律

- 只凭单帧不能判断运镜；至少比较首中尾三帧，仍不明确就看短片。
- 先区分主体运动与摄影机运动。背景视差整体变化更支持摄影机位移；主体局部变化更支持主体运动。
- `zoom` 是焦距变化，`push/pull` 是摄影机位移；无法确认时写 `push-or-zoom (uncertain)`。
- 一镜包含多个运动时按时间顺序记录，不把多个相互冲突的运镜词堆在一起。

## 长镜头与快切补帧

- 单镜超过 6 秒，或关键动作只在中间发生，使用 `--extra-frame-interval 2.0` 重跑。
- 快切、闪白、卡点剪辑使场景检测不稳时，调整阈值后保留两次结果进行比对。
- 自动切镜只是候选边界。视觉复核发现误切时，在最终 `analysis.json` 合并或拆分，并记录 `boundary_adjustment`。

## 一致性锚点素材

为第四式记录可跨镜复用的固定描述：

- `named_subjects`：角色或产品的稳定名称。
- `original_lock`：外观、服饰、材质、比例和独特特征的 1–2 句固定描述。
- `style_lock`：跨镜重复的 2–4 个光色、镜头和质感锚词。
- `reference_frames`：最能锁定主体与风格的帧路径。
- `prev_shot_token`：前一镜的终态、位置、动作方向和光照状态。

不要为无授权的真实人物创建可用于冒充身份的面部锁定描述。

