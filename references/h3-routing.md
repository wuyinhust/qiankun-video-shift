# MiniMax H3 官方变招路由

本参考只在第四式需要 MiniMax H3 输出时读取。格式权威是 MiniMax 官方 [`h3-prompt-writing`](https://github.com/MiniMax-AI/MiniMax-H3/tree/main/skills/h3-prompt-writing) Skill；本文件只负责把本 Skill 的分析结果路由给它，不复制官方完整指南。

## 权威优先级

1. 官方 `$h3-prompt-writing` 的字段名、顺序、标签和时间写法。
2. 本 Skill 的已验证分镜事实和一致性锚点。
3. 第三式的爆款结构约束。
4. 第四式的增强建议。

若通用模板与官方 H3 契约冲突，以官方契约为准。不得擅自添加官方格式之外的顶层字段。

## 模式选择

| 输入关系 | H3 模式 | 关键用法 |
|---|---|---|
| 只有文字母版 | T2VA | 从文本构建完整视听时间线 |
| 使用抽取的首帧 | I2VA | 首帧在 0.00 秒完全对齐，并从该状态向前发展 |
| 使用首帧和尾帧 | FL2VA | 描述两帧之间连续、可达的动作路径 |
| 只使用尾帧 | L2VA | 推断合理前态并逐步收敛到尾帧 |
| 原视频/图片/音频承担完整参考关系 | Ref2VA | 用 Subject/Picture/Video/Audio 标签描述保留与迁移关系 |

对标视频反推默认优先评估 Ref2VA。仅把关键帧作为生成锚点、不把源视频整体作为参考时，选择 I2VA 或 FL2VA。

## Base 模式输入映射

把当前生成单元映射为：

- `integrated_multimodal_description`：逐镜时间线、可见动作、摄影机、台词和画内声音。
- `overall_soundscape`：环境声、物理动作声和非语言人声的整体概括。
- `non_diegetic_music`：角色听不到、仅观众听到的配乐；没有则 `N/A`。

I2VA、FL2VA、L2VA 的图片对齐指令必须由官方 Skill 原样生成。切镜时间严格递增并落在当前单元时长内。

## Ref2VA 输入映射

把分析结果映射为官方六段：

- `subject_definitions`：需要跨段追踪的主体、画面锚、原视频结构和音频角色。
- `summary`：任务类型与主要参考关系。
- `retention_analysis`：逐标签说明完全保留、部分保留、属性迁移或弱参考。
- `detailed_description`：逐镜播放顺序、动作、摄影机、声音和引用生效点。
- `overall_soundscape`。
- `non_diegetic_music`。

标签保持全局一致：`<Subject N>`、`<Picture N>`、`<Video N>`、`<Audio N>` 不得中途改义或重新编号。

## 时间与语言

- 每个 H3 生成单元总时长 4–15 秒。
- 官方重写区段用英文。
- 台词、歌词和画面可见文字保留原语言与原意；未经授权的原视频文案需要原创改写后再写入。
- 台词使用稳定说话人 ID，并保持口型、画外音和跨切点连续规则。
- 抽象词必须落到构图、动作、摄影机、声音和时间点。

## 长视频分段

1. 优先在第三式识别出的功能边界切分，如钩子结束、证明开始、转变发生、CTA 进入。
2. 每段 4–15 秒；不足 4 秒的碎片与相邻功能段合并。
3. 不跨越无法连续生成的硬切结构，除非 H3 模式明确需要多镜头。
4. 每段继承 `Original Lock` 和 `style_lock`，并把上一段终态写成 `Prev-shot Token`。
5. 输出一个段落索引，标明源时间范围、H3 模式、参考素材和生成时长。

## 交付检查

- 已实际加载并遵循官方 `$h3-prompt-writing`。
- 模式与素材关系一致。
- 字段名、顺序和标签完全正确。
- 所有切镜时间在单元时长内且严格递增。
- 声音分类正确：台词/画内声、整体声景、画外配乐不互相重复。
- 每个提示词完整展示，不用省略号截断。

