---
name: qiankun-video-shift
description: "从用户提供的本地文件或公开链接获取对标短视频，自动分镜抽帧，拆解可迁移的爆款结构，并还原为通用及 MiniMax H3 视频提示词。用于对标视频、爆款拆解、视频反推提示词、分镜关键帧、同结构原创改编；不用于单纯剪辑、绕过平台访问限制或照搬他人素材与身份。"
---

# 乾坤大挪移

把一条对标视频变成可核验的分镜证据、可复制的爆款公式和可直接使用的视频生成提示词。全程遵循“外层确定、内层智能”：脚本负责下载、探测、切镜、抽帧和时间戳；Agent 负责观察、判断、解释与提示词创作。

对用户展示工作流时，阶段统一称为“招式”。

四式取义于《倚天屠龙记》对乾坤大挪移机理的描述：发挥并积蓄潜力、转换刚柔阴阳、寻瑕抵隙、牵引挪移敌劲。前两式据此映射到视频领域，后两式沿用原著概念；不要另造与这些机理无关的武侠名目。

## 总诀

严格按四式执行：

1. **第一式·蓄力纳影**（对标）：取得用户指定的视频，保存来源与文件指纹；如同先完整蓄住来劲，不在入口损失信息。
2. **第二式·刚柔析镜**（拆解）：自动分镜、抽取首中尾关键帧，建立逐镜时间轴；把静与动、明与暗、硬切与柔转分别辨清。
3. **第三式·寻瑕抵隙**（分析）：基于可见证据识别关键时刻，提炼可迁移公式并指出风险；同时找出吸引力支点与结构破绽。
4. **第四式·牵引挪移**（还原）：先忠实还原，再把可迁移之“劲”导向增强、一致性设计和目标模型编译；MiniMax H3 使用官方 `h3-prompt-writing` 契约。

不要把三个上游 Skill 各跑一遍。所有阶段共用同一份 `shot_manifest.json` 与 `analysis.json`，避免重复观察和结论冲突。

## 起手检查

执行脚本前运行：

```bash
python3 scripts/check_environment.py --require-core
```

核心媒体路径需要 `ffmpeg` 与 `ffprobe`。链接来自短视频或社交平台时还需要 `yt-dlp`；直接视频 URL 可由标准库下载。允许用 `FFMPEG_BIN`、`FFPROBE_BIN`、`YTDLP_BIN` 指向未加入 PATH 的可执行文件。

缺少依赖时说明缺什么及受影响的招式，取得用户同意后再安装。不得静默安装系统软件、读取浏览器 Cookie 或使用用户未授权的账号凭据。

媒体脚本的完整用法和目录约定见 [references/media-pipeline.md](references/media-pipeline.md)。

## 第一式·蓄力纳影（对标）

接受两种入口：

- **本地文件**：使用用户给出的路径；默认不复制原文件，只生成来源清单。
- **公开链接**：仅下载用户有权使用、无需绕过权限或 DRM 的内容。平台页面优先 `yt-dlp`，直接媒体地址走 HTTP 下载。

运行：

```bash
python3 scripts/acquire_video.py "<本地路径或 URL>" --output-dir "<本次工作目录>/source"
```

必须产出 `source_manifest.json`，至少记录脱敏后的来源、最终本地路径、SHA-256 和文件大小。链接中的 query 与 fragment 不得写入报告，防止泄露签名参数或 token。

如果链接需要登录、Cookie、验证码、付费权限或绕过平台限制，停止并让用户改为自己下载后提供本地文件。

## 第二式·刚柔析镜（拆解）

运行：

```bash
python3 scripts/segment_video.py "<视频路径>" --output-dir "<本次工作目录>/deconstruction"
```

该脚本必须产出：

- `shot_manifest.json`：媒体参数、有效分析时长、切镜阈值和逐镜时间戳。
- `frames/`：每镜首帧、中帧、尾帧；同一时间点自动去重。
- `frame_index.html`：按镜头与时间码浏览关键帧。
- `audio.wav`：源视频有音轨且未关闭音频提取时生成。

Agent 必须实际查看所有镜头的关键帧；不可只读文件名或让技术信号代替视觉观察。对长镜头、快动作、复杂转场或关键动作不明的镜头，再查看对应短片或提高抽帧密度。宿主能直接读取视频时，可以同时查看整段视频，用于判断速度、运镜、动作连续和声音，但整段直读不能替代受控抽帧。

逐镜补全：客观画面、景别、机位、构图、主体与动作、运镜、光色、字幕、台词/声音、转场和证据帧。事实与推断分开；看不清就标记 `unknown`，不要猜品牌、人物、曲名或数据。

分析维度见 [references/shot-analysis.md](references/shot-analysis.md)，统一字段见 [references/output-contract.md](references/output-contract.md)。

## 第三式·寻瑕抵隙（分析）

读取 [references/viral-framework.md](references/viral-framework.md)，只基于第二式的镜头事实和用户提供的真实背景进行判断。

至少交付：

- 前 3 秒钩子与停留机制。
- 3–8 个关键时刻及精确时间范围。
- 叙事结构、核心吸引力、目标受众和观看心理。
- 一句话爆款公式，以及 3–5 条可迁移模式。
- 2–4 条潜在隐忧，防止只夸不判。
- “可迁移”与“不可照搬”边界。

关键时刻使用受控枚举：`hook`、`curiosity_gap`、`pain_point`、`relatability`、`authority`、`social_proof`、`super_effect`、`transformation`、`twist`、`urgency`、`value`、`cta`。允许主类型加一个副类型，但不可为凑齐枚举而虚构片段。

评分保持克制：8 分以上需要明显证据，9 分以上只用于现象级表现。没有播放、完播、互动等真实数据时，只能称为“结构潜力评分”，不得断言视频实际爆过。

## 第四式·牵引挪移（还原）

先读取 [references/reconstruction.md](references/reconstruction.md)，并遵守以下顺序：

1. **还原版**：只写原片成立所必需的主体、场景、动作、时序、运镜、光色和声音，不擅自美化。
2. **增强版**：在还原版上加入更明确的运动设计、光色层次、节奏、负向约束和原创替换。
3. **三变式**：构图、光影、风格各只改一个变量，便于 A/B 测试。
4. **一致性锚点**：命名主体、Original Lock、Prev-shot Token、风格锁和参考帧映射。
5. **模型编译**：先保留通用母版，再输出目标模型格式。

### MiniMax H3 变招

目标为 MiniMax H3，或用户未指定模型但明确要求 H3 版本时，读取 [references/h3-routing.md](references/h3-routing.md)，并使用已安装的官方 `$h3-prompt-writing` Skill 作为最终格式权威。

- 无参考素材：T2VA。
- 首帧：I2VA。
- 首尾帧：FL2VA。
- 仅尾帧：L2VA。
- 原视频、图片或音频作为完整参考关系：Ref2VA。

H3 官方字段名、顺序、标签和时间表示优先于本 Skill 的通用模板。源视频超过 H3 单段有效时长时，按镜头语义切成多个 4–15 秒生成单元，每个单元单独编译，并用一致性锚点连接。

## 标准交付包

默认交付：

1. 来源与技术摘要。
2. 完整分镜表、时间戳和关键帧索引。
3. 爆款结构、关键时刻、公式、风险和原创迁移说明。
4. 通用还原版 Prompt。
5. 通用增强版 Prompt 与三变式。
6. 一致性锚点。
7. MiniMax H3 官方格式 Prompt；用户指定其他模型时再追加对应版本。
8. `analysis.json` 与面向用户的 `report.md`。

写完 `analysis.json` 后运行：

```bash
python3 scripts/validate_analysis.py "<analysis.json>" --check-files
```

校验失败时修复列出的具体字段，不要整体推倒重做。

## 原创、安全与权限边界

- 迁移钩子、节奏、镜头语法、信息顺序和可信度装置；不要复制原素材、品牌、Logo、水印、字幕原文、音乐、台词、虚假商品声明或具名创作者身份。
- 不生成用于冒充真实人物的身份锁定、声音克隆或欺骗性深伪提示词。用户有权使用本人素材时可保留非欺骗性的本人一致性。
- 看到未成年人时不建立可复刻其身份的面部锚点。
- 下载、安装依赖、调用收费 API、上传媒体或向生成平台提交任务，都需要分别符合用户授权；本 Skill 本身不推定这些权限。
- 默认本地处理。若要上传第三方模型，先说明发送哪些文件、用途和隐私影响。

## 参考来源

融合方法与上游边界见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
